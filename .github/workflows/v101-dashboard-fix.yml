name: V10.1 Dashboard Fix

on:
  push:
    branches: [main]
    paths:
      - "tools/v101_dashboard_fix_patch.py"
      - ".github/workflows/v101-dashboard-fix.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply V10.1 dashboard fix
        run: python tools/v101_dashboard_fix_patch.py
      - name: Validate Python
        run: python -m py_compile app.py
      - name: Commit app update
        run: |
          if git diff --quiet -- app.py; then
            echo "No app.py changes required"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.py
          git commit -m "Fix V10 dashboard KPI rendering and live states"
          git pull --rebase origin main
          git push origin main
